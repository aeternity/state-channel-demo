{
  "name": "rock-paper-scissors",
  "version": "1.0.0",
  "description": "This is the default package.json generated for your project",
  "scripts": {
    "lint": "eslint",
    "test": "mocha ./test/**/*.test.js --timeout 0 --exit",
    "test:bail": "mocha ./test/**/*.test.js --timeout 0 --exit -b --failed-first",
    "test:debug": "node --inspect-brk node_modules/.bin/mocha ./test/**/*.test.js --timeout 0 --exit -b --failed-first",
    "deploy": "node test/deploy.js"
  },
  "dependencies": {
    "@aeternity/aepp-sdk": "^12.0.0"
  },
  "devDependencies": {
    "@aeternity/aeproject": "^4.1.0",
    "@typescript-eslint/eslint-plugin": "^5.30.4",
    "@typescript-eslint/parser": "^5.30.4",
    "chai": "^4.3.6",
    "eslint": "^8.19.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-mocha": "^10.0.5",
    "mocha": "^10.0.0"
  }
}
